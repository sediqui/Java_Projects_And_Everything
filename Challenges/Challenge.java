class Challenge {
  public static void main(String[] args) {
    // Student student = new Student("Alice", 20);
    // System.out.println(student);
  
    // 71. Concatenate and Convert: Take two strings, concatenate them, and convert the result to uppercase.
    // String str1 = "Hello,";
    // String str2 = " World";
    // String result = str1 + str2;
    // System.out.println(result.toUpperCase());
    // System.out.printf("%s%n", result.toUpperCase());
    // System.out.printf("%S%n", result);

    // 72. Calculate the area and circumference of a circle for a given radius using Math.PI
    // double radius = 5.0;
    // double area = Math.PI * radius * radius;
    // double circumference = 2 * Math.PI * radius;
    // System.out.printf("Area: %.2f%n", area);
    // System.out.printf("Circumference: %.2f%n", circumference);

    // 73. Simulate a dice roll using Math.random() and display the outcome (1 to 6).
    // int diceRoll = (int) (Math.random() * 6) + 1;
    // System.out.println("Dice roll outcome: " + diceRoll);

    // 74. Create a number guessing game where the program selects a random number, and the user has to guess it.
    // int randomNumber = (int) (Math.random() * 6) + 1;
    // int userGuess = 3; // Example guess, replace with user input in a real application
    // if (userGuess == randomNumber) {
    //   System.out.println("Congratulations! You guessed the correct number: " + randomNumber);
    // } else {
    //   System.out.println("Sorry, the correct number was: " + randomNumber);
    // }

  // 75. Take an array of words and concatenate them into a single string using StringBuilder.
  // String[] words = {"Hello", " ", "World"};
  // StringBuilder sb = new StringBuilder();
  // for (String word : words) {
  //   sb.append(word);
  // }
  // System.out.println(sb.toString());
  // System.out.println(sb);

  // FinalObject obj = new FinalObject("Final Value", 42);
  // System.out.println(obj.getName() + ", " + obj.getAge());

  }
}
// 70. Define a Student class with fields like name and age, and use toString to
// print student details.
// class Student {
//   private String name;
//   private int age;
//   public Student(String name, int age) {
//     this.name = name;
//     this.age = age;
//   }
//   @Override
//   public String toString() {
//     return "Student{name='" + name + "', age=" + age + "}";
//   }
// }

// 76. Create an object with final fields and a constructor to initialize them.
// class FinalObject {
//   private final String name;
//   private final int age;
//   public FinalObject(String name, int age) {
//     this.name = name;
//     this.age = age;
//   }
//   public String getName() {
//     return name;
//   }
//   public int getAge() {
//     return age;
//   }
// }
